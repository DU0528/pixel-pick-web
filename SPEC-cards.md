# 색깔 카드 뒤집기 (컬러 메모리 매칭) 게임 스펙

> 작성일: 2026-06-16
> 상태: Draft
> 대상 프로젝트: pixel-pick-web (백엔드 없는 정적 사이트)

## 한 줄 요약
같은 색 카드 짝을 기억으로 찾아내는 메모리 매칭 게임. 1~3인 핫시트 플레이, 4×4·6×6·8×8 그리드 선택.

## 배경 & 목적
- 기존 `pixelcolorpick` 사이트(색감 도구 + 색깔 맞추기 게임)에 **두 번째 미니게임** 추가.
- 색을 테마로 한 캐주얼 메모리 게임으로 체류시간·재방문 유도.
- 성공 기준(정성): 4×4를 모바일에서 끊김 없이 한 손으로 플레이 가능, 8×8을 데스크탑에서 색 구분 혼란 없이 클리어 가능.

## 범위 (Scope)

### In Scope (MVP)
- 그리드 3종: 4×4(8쌍) / 6×6(18쌍) / 8×8(32쌍)
- 짝 판정: **같은 hex 색쌍, 시스템 자동 판정**(위치 기억 게임)
- 1~3인 **핫시트**(한 기기 번갈아) 플레이 + 플레이어 이름 입력
- 적응형 색 팔레트(그리드 클수록 구분성↑)
- 1인 모드: 클리어 시간 기록 + **최고 기록 localStorage 저장**
- 멀티: 짝 수 승부, 동점 시 **서든데스(2×2 새 판, 먼저 1쌍)**
- 색약 토글(스와치에 숫자/기호 보조 표시)
- 결과 **카카오 공유**(기존 게임과 동일 패턴)
- 별도 페이지 `cards.html`, 기존 헤더/CSS/favicon 재사용, 도구/게임 카드에서 링크 연결

### Out of Scope (추후)
- **온라인 대전**(네트워크 동기화) — 구조는 확장 여지만 남김(상태/턴 로직을 UI와 분리)
- 계정/서버 리더보드(전역 랭킹)
- 카드 테마/스킨, 사운드, 애니메이션 고급화

## 기능 요구사항

### 핵심 기능
1. **셋업 화면**: 플레이어 수(1/2/3) · 그리드(4·6·8, 단 모바일은 4×4만) · 플레이어 이름 입력 · 색약 토글 → 시작.
2. **덱 생성**: (그리드²/2)쌍의 같은 색 카드 2장씩 생성 후 셔플.
3. **턴 흐름**:
   - 카드 클릭 → 뒤집어 색 공개(1장째).
   - 2장째 공개 → **같으면**: 두 장 `matched` 처리(수집), 현재 플레이어 점수+1, **같은 사람이 계속** 진행.
   - **다르면**: 약 **0.8초 색 노출 후** 둘 다 다시 덮고 **다음 플레이어로** 턴 이동. 노출 중에는 **입력 잠금**.
4. **진행 표시**: "지금 ○○ 차례" + 각 플레이어 실시간 점수(먹은 짝 수). 1인은 경과 타이머.
5. **종료 판정**: 모든 짝 수집 시 종료.
   - 1인: 클리어 시간 표시 + 최고 기록 갱신 시 강조.
   - 멀티: 짝 수 랭킹. 1위 동점자 ≥2명 → **서든데스**.
6. **서든데스**: 동점자만 참가. 2×2(2쌍) 새 판을 깔고 번갈아 뒤집어 **먼저 1쌍 맞춘 사람 승**.
7. **결과 화면**: 승자/기록 표시 + 카카오 공유 + 다시하기.

### 부가 기능
1. 색약 토글: 각 색 스와치에 동일 색=동일 기호(숫자/심볼) 오버레이.
2. 최고 기록(그리드별 1인 베스트 타임) localStorage 저장·표시.

## 기술 요구사항

### 데이터 모델 (클라이언트 메모리)
```
Card    { id, pairId, color(hex), symbol?, state: 'hidden'|'revealed'|'matched', pos }
Player  { name, score }            // score = 수집한 짝 수
Game    {
  gridSize(4|6|8), mode('solo'|'multi'),
  deck: Card[], flipped: Card[](≤2),
  players: Player[], turn: idx, lockInput: bool,
  colorblind: bool, startTime, elapsed,
  phase: 'setup'|'play'|'suddenDeath'|'result'
}
```
- 턴/판정 로직을 **순수 함수**로 분리 → 추후 온라인화 시 서버 권위 로직으로 재사용.

### 색 팔레트 생성 (핵심 알고리즘)
- N = 그리드²/2 (8 / 18 / 32).
- **색상환 hue 균등 분할**을 기본 축으로, 채도(S)·명도(L)를 **그리드가 클수록 넓게** 분포시켜 N색이 서로 구분되게 함(적응형 C안).
- 기존 `game.js`의 HSL→HEX 유틸/난수 로직 재사용 검토(중복 구현 금지). 4×4 등 작은 판은 통일된 톤으로 예쁘게.
- 인접 색 간 최소 지각 거리 확보(필요 시 HSL 거리/간단 ΔE 근사로 재생성).

### 상태 흐름 / 동시성
- 핫시트라 진짜 동시성은 없음. 단 **연타/노출 중 클릭** 방지 위해 `lockInput`으로 입력 게이트.
- 무시 케이스: 이미 `matched`/`revealed`인 카드 클릭, 같은 카드 2번째 클릭, 잠금 중 클릭.

### 성능 기준
- 최대 64 카드 = DOM 64노드. 성능 이슈 없음. 카드 뒤집기는 CSS 3D transform.
- 타이머는 `requestAnimationFrame`/`setInterval` 1초 갱신.

### 의존성 & 리스크
- 외부 의존: 카카오 JS SDK(이미 사용 중). 그 외 없음.
- **최대 리스크**: 8×8(32색)에서 **메모리로 색 구분이 어려움** → 적응형 팔레트 + 색약 기호로 완화. 그래도 어려우면 8×8 기본 색약 기호 ON 고려(미결).

## UI/UX 명세

### 주요 화면 흐름
1. 셋업 → 2. 플레이(보드 + 턴/점수 HUD) → 3. (동점) 서든데스 → 4. 결과(공유/재시작).

### 엣지 케이스 처리
| 상황 | 처리 |
|------|------|
| 노출(0.8s) 중 다른 카드 클릭 | 입력 잠금, 무시 |
| 이미 맞춘/공개된 카드 클릭 | 무시 |
| 같은 카드 연속 2번 클릭 | 2번째 무시 |
| 모바일 세로에서 6×6·8×8 선택 시도 | 선택 비활성(4×4만 노출) |
| 새로고침/뒤로가기 중간 이탈 | 상태 소실(MVP 허용), 진행 중이면 이탈 확인 안내 |
| 멀티 3인 동점·서든데스도 동점 | 서든데스 반복(먼저 1쌍 판정상 동점 불가에 가깝지만 재시도) |

### 접근성
- 색약 토글로 색+기호 이중 단서.
- 카드 키보드 포커스/Enter 뒤집기 지원(가능 범위), 적절한 aria-label.
- 충분한 탭 타깃(모바일 4×4 한정으로 확보).

## 트레이드오프 & 결정 사항
- **같은 hex 자동판정(A)** 채택: "비슷한 색 고르기(B)"는 32쌍에서 플레이 붕괴 → 기억 게임으로 확정.
- **모바일 4×4 한정**: 64칸 손톱 타깃 문제 회피. 큰 판은 큰 화면 전용.
- **핫시트 우선**: 인프라 0. 온라인은 로직 분리로 확장만 대비.
- **서든데스(2×2)**: 시간 비교보다 직관적이고 긴장감↑.

## 실패 대응
- 정적 프런트엔드라 배포 리스크 낮음. 감지: 콘솔 에러/수동 QA(4·6·8 각 그리드 1·2·3인 플레이).
- 롤백: 정적 파일이므로 이전 커밋/배포로 되돌리기. 데이터 마이그레이션 없음(localStorage 키는 신규).

## 미결 사항 (Open Questions)
- [ ] 8×8 기본 색약 기호 ON 여부(가독성 vs 순수 색감)
- [ ] 카카오 공유 카드 문구/이미지(기존 게임 카드 재사용 or 신규)
- [ ] 최고 기록을 그리드별로만 저장할지, 인원수까지 구분할지

## 구현 우선순위
| 우선순위 | 항목 | 복잡도 |
|---------|------|-------|
| P0 | 셋업·덱 생성·셔플·보드 렌더 | 중 |
| P0 | 턴/판정 로직(매치=연속, 미스=0.8s 후 턴이동, 입력잠금) | 중 |
| P0 | 적응형 색 팔레트 생성(기존 유틸 재사용) | 중상 |
| P0 | 1인 타이머·종료, 멀티 점수·랭킹 | 중 |
| P1 | 서든데스(2×2) | 중 |
| P1 | 색약 토글(기호 오버레이) | 하 |
| P1 | 최고기록 localStorage, 카카오 공유, 사이트 링크 연결 | 하 |
| P2 | 키보드/aria 접근성, 이탈 확인 | 하 |

## 산출물 위치
- 신규: `cards.html`, `cards.js`, (스타일은 기존 CSS 재사용 + 필요한 추가분).
- 연결: `index.html`/`game.html`의 도구·게임 카드에 진입 링크 추가.
